instrumentation:
  enabled: true
  http:
    payloads: true
    headers:
      - ot-tracer-spanid
  db:
    statement_values: true
metadata:
  extra: value
  more: value2
code_path:
  enabled: true
runner:
  enabled: true
  exclude_branches:
    - master
